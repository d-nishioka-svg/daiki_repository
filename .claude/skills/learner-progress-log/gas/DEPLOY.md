# 導入手順

`Code.gs` を、学習進捗管理スプレッドシートに紐づけて使えるようにする手順です。2つの使い方があります。

- **A. メニューから手動で貼り付け(初回から使える、AI不要)** — Claudeが作った記録文をコピペするだけの方式
- **B. Webアプリ経由でVTTから自動作成(要: GeminiRaytechライブラリ、Web App配布設定)** — VTTファイルをアップロードすると、AIが要約案を作り、内容を確認してから書き込める方式。企業・受講者の新規登録や、集団相談用のグループ管理もこのWebアプリから行える。

Bはコードを追加しただけでは使えず、**Webアプリとしてのデプロイ**と**GeminiRaytechライブラリの追加**が別途必要です。以下、順番に説明します。

## 1. スプレッドシートにApps Scriptを追加する(A・B共通)

1. 学習進捗管理スプレッドシートをGoogleスプレッドシートで開く
2. メニューの「拡張機能」→「Apps Script」を開く(新しいタブでエディタが開きます)
3. 最初から入っている `function myFunction() {}` などのコードを全部削除する
4. このフォルダの `Code.gs` の中身を全部コピーして貼り付ける
5. このフォルダの `appsscript.json` の中身も、エディタ左側の「プロジェクトの設定」(⚙️)で
   「"appsscript.json" マニフェスト ファイルをエディタで表示する」にチェックを入れると
   編集できるようになるので、同様に中身を貼り替える(GeminiRaytechライブラリの依存関係が
   含まれているため。手順2でライブラリを追加した場合は上書きしなくてもよい)
6. 上部の保存アイコン(💾)で保存する(プロジェクト名は何でもよい。例:「学習進捗ログ更新」)

## 2. Aの使い方(メニューから手動貼り付け)

保存したら、スプレッドシートのタブに戻り、**一度ページを再読み込み**してください。数秒後、メニューバーに
「**学習進捗ログ**」という新しいメニューが追加されます。これで準備完了です。デプロイ作業は不要です。

初回だけ、メニューの「記録を追加」を押した際に権限の承認画面が出ます。自分のGoogleアカウントで許可してください
(「このアプリは確認されていません」という警告が出ても、自分で作ったスクリプトなので「詳細」→「(プロジェクト名)に
移動」で進めて問題ありません)。

使い方:

1. Claudeに、Zoomの相談会のVTTファイル・企業名・受講者名を渡す
2. Claudeが記録文を作成し、確認を求めてくるので内容をチェックする
3. OKなら、スプレッドシート側で「学習進捗ログ」→「記録を追加」を開く
4. 出てきたダイアログで、企業(シート)と受講者をプルダウンから選び、Claudeが作った文章を「記録内容」欄に貼り付ける
5. 集団相談の場合は「+ 対象者を追加」で行を増やし、参加者ごとに(同じ文章を)企業・受講者を選んで貼り付ける
6. 「この内容で書き込む」を押すと、選んだ受講者の列の一番下に追記される。結果(成功/失敗)がダイアログ内に表示される

## 3. Bの使い方(Webアプリ経由でVTTから自動作成)

### 3-1. GeminiRaytechライブラリを追加する

要約の生成には、個人のGemini APIキーではなく、社内のAI推進室が用意したGASライブラリ
「**GeminiRaytech**」を使う(社内のGemini Gateway移行方針に沿った、GAS向けの正式な接続方法。
APIキーの発行・保管は一切不要)。

1. 利用には所定の権限が必要。**AI推進室(AIチーム)に利用権限の付与を依頼する**
2. Apps Scriptエディタ左側の「ライブラリ」の➕ボタンをクリック
3. スクリプトIDに `1SNn6G_ri9HwMu1jLLoA7ChZVVlS4Vk_LYwUNIWhvBvpYenikR1LrWKcW` を入力して検索
4. バージョンが「2」になっていることを確認して追加
5. ライブラリ欄に「GeminiRaytech」が表示されれば設定完了(詳細・トラブルシューティングは
   社内ドキュメント「GASライブラリ「GeminiRaytech」の使い方」を参照)

### 3-2. 初回権限承認(重要)

Webアプリ経由の初回実行では権限承認のポップアップを出せないため、**先にApps Scriptエディタ側で
手動実行して承認を済ませておく**必要がある。

1. Apps Scriptエディタ上部の関数選択プルダウンで `testGeminiRaytech_` を選ぶ
2. 実行ボタン(▶️)を押す
3. 「承認が必要です」のポップアップが出たら「権限を確認」→ 自分のアカウントを選択 →
   「Googleアカウントへのアクセスを求められる」画面で**全てのチェックボックスにチェックを
   入れて**「続行」
4. 実行ログに何かしらのテキスト(Geminiからの応答)が出ていれば成功

401/403エラーが出る場合は、権限付与(手順3-1の1)が済んでいるか、appsscript.jsonの
`oauthScopes`を独自に上書きしていないかを確認する(詳細は社内ドキュメント参照)。

**補足(実際にハマったポイント)**

- `testGeminiRaytech_` のように名前の末尾が `_` の関数は、GASエディタの実行関数プルダウンに
  **表示されない**(GASの仕様)。見当たらない場合は、末尾に `_` が付かないテスト用関数
  (例: `testGemini`、Code.gs内に用意済み)を代わりに選んで実行すること。
- appsscript.jsonの設定・初回承認とも問題ないのに`ACCESS_TOKEN_SCOPE_INSUFFICIENT`
  (403)が解消しない場合、原因はほぼ確実に**AI推進室側でのこのアカウントへの利用許可が
  未完了**であることだった。一般的な情報システム部の窓口に問い合わせても、
  「GeminiRaytechの利用権限(AI推進室が管理しているaiplatform.googleapis.comへの
  アクセス)」だと明示しないと、別件の権限を再付与されて解決しないことがある。
  エラー文言(`reason: ACCESS_TOKEN_SCOPE_INSUFFICIENT`)をそのまま伝えるとよい。
- 上記が解決した直後は、今度は`404 Publisher model ... was not found`が出ることがある。
  これは権限の問題ではなく、`generateText`/`generateContent`のmodelId省略時に
  ライブラリ側が使うデフォルトモデル(実測では`gemini-2.0-flash`。社内ドキュメント記載の
  `gemini-2.5-flash`とは異なっていた)が、このプロジェクトでは有効化されていないために起きる。
  **modelIdは必ず明示的に指定すること。** そのため`Code.gs`の先頭で
  `GEMINI_MODEL_DEFAULT`(現在は`gemini-3.6-flash`)を定義し、`callGemini_`・
  `testGeminiRaytech_`・`testGemini`のすべてがこの定数を使うようにしてある。スクリプト
  プロパティの設定は不要(設定は任意で、`GEMINI_MODEL`を入れた場合はそちらが優先される)。
- **一度動いていたのに、あとから急に`ACCESS_TOKEN_SCOPE_INSUFFICIENT`(403)が再発した場合。**
  これはコードの問題ではなく、スクリプトに与えられたアクセストークンに
  `cloud-platform`スコープが乗っていないという意味。まず**エディタから`testGemini`を実行**して、
  どちらの状況か切り分ける:
  - エディタでは成功する → スクリプトへの承認は生きていて、**Webアプリのデプロイ側**が古い
    承認状態で動いている。承認ポップアップはWebアプリからは出せないため、
    `google.script.run`は黙って権限不足のまま失敗する(「エディタでは動くがアプリでは落ちる」)。
    エディタで権限確認ポップアップの**全チェックをONにして承認**し直したうえで、
    「デプロイを管理」→鉛筆→「新バージョン」→「デプロイ」で作り直す
  - エディタでも同じ403が出る → 承認そのものが失われている。心当たりとしては
    https://myaccount.google.com/connections でこのアプリのアクセスを削除した、
    AI推進室側の利用許可が外れた、など。前者なら再承認で直り、後者ならAI推進室への再依頼が要る
- **スクリプトが使うGoogleサービスを増やすと、この403が起きうる。** 必要スコープが変わると
  承認のやり直しが必要になり、Webアプリ側だけが古い権限のまま落ちるため。
  新しいサービス(`Session`など)を使う前に、既に使っている`SpreadsheetApp`側で代用できないか
  検討すること(例: タイムゾーンは`Session.getScriptTimeZone()`ではなく
  `ss.getSpreadsheetTimeZone()`で取れる)。追加が避けられない場合は、
  **コードを反映したあと必ずエディタから一度実行して承認し直してから**再デプロイすること。
- モデルを変えたくなった場合は、`Code.gs`の`GEMINI_MODEL_DEFAULT`を書き換える。有効なモデル名は
  https://cloud.google.com/vertex-ai/generative-ai/docs/models の「Model ID」欄を直接
  確認すること(Google検索のAI概要は実在しないモデル名を出すことがあったため信用しない)。

### 3-3. Webアプリとしてデプロイする

1. Apps Scriptエディタ右上の「デプロイ」→「新しいデプロイ」
2. 種類の選択で「ウェブアプリ」を選ぶ
3. 「次のユーザーとして実行」: **自分**
4. 「アクセスできるユーザー」: **当社内の全員**(「全員」という匿名アクセスの選択肢は会社のWorkspaceポリシー上出てこないが、これで問題ない。使うのは自分だけのため)
5. 「デプロイ」をクリックし、認証を求められたら許可する
6. 発行された **ウェブアプリのURL**(`.../exec` で終わるもの)をブックマークしておく

### 3-4. コードを更新したとき(B利用時のみ重要な注意点)

`Code.gs` の中身を直しても、**保存しただけではWebアプリ側には反映されません**。以下の手順で再デプロイが必要です:

1. 「デプロイ」→「デプロイを管理」
2. 該当のデプロイの鉛筆(編集)アイコンをクリック
3. 「バージョン」を「新バージョン」に変更
4. 「デプロイ」をクリック

(URLは変わらないので、ブックマークはそのままでよい)

### 3-5. 使い方(記録を追加・1件ずつ)

Webアプリの上部に「記録を追加」「進捗を確認」「登録・管理」の3つのタブがある。まず「記録を追加」タブ:

このタブには2つのモードがあり、見出しのすぐ下のトグルで切り替える。

- **1件ずつ(個別・集団相談)** … VTT 1本を1人、または集団相談の参加者全員に書き込む(以下の手順)
- **複数VTTを一括(個別相談)** … 個別相談のVTTを何本もまとめて処理する(3-5-1を参照)

1. ブックマークしたウェブアプリのURLを開く
2. 集団相談で、対象者があらかじめ「グループ」として保存済みなら、上部の「グループから対象者を
   読み込む」で選んで「このグループを対象者欄に読み込む」を押すと、対象者欄が自動で埋まる
   (手順3を省略できる)
3. 「VTTファイル」欄はクリックして選択するか、ファイルをそのままドラッグ&ドロップしてもよい。
   ファイル名から実施日を推測できた場合(Zoomの`GMT20260827-...`形式、または`2026-08-27`の
   ような形式が含まれる場合)は「実施日」欄に自動で入力される(推測が外れている場合は手動で
   修正すること)。推測できなかった場合は自分で入力する
4. 「企業(シート)」「受講者」をプルダウンで選ぶ(集団相談の場合は「+ 対象者を追加」で人数分の
   行を増やす)。個別相談で1人だけ書き込んだ直後は、次回このタブを開いたときに前回と同じ
   企業・受講者が自動で選択された状態になる(隔週で同じ人と続けて相談する運用を想定した
   ブラウザ内の記憶で、他の人には影響しない。集団相談時は反映されない)
5. 「AIで要約を作成」を押す(数十秒かかることがある)。記録内容欄にAIが作った下書きが入る
6. **内容を必ず確認し、必要なら手直しする**(先方企業への報告に使われるため)
7. 次回の相談日が決まっていれば「次回相談予定日」に日付・開始・終了を入れる(任意)。
   ここに入れた内容は、書き込み時に「次回日程一覧」シートへ自動で反映される(3-5-4を参照)
8. 「この内容で書き込む」を押すと、選んだ受講者の列に追記される。結果(成功/失敗)が画面下に表示される

### 3-5-1. 使い方(複数VTTを一括)

個別相談を何人分か続けてやった日に、VTTをまとめて処理するためのモード。
**ファイル1本 = 受講者1人**として扱う(集団相談には使わない。集団相談は3-5の「1件ずつ」で行う)。

1. 「複数VTTを一括(個別相談)」に切り替える
2. VTTをまとめて選択、またはドラッグ&ドロップする。ファイル1本につきカードが1枚並ぶ
   (「1件ずつ」側のドロップ欄に複数ファイルを落とした場合も、自動でこのモードに切り替わる)
3. カードごとに、以下が自動で入る:
   - **実施日** … ファイル名から推測(3-5の手順3と同じ判定)
   - **企業・受講者** … 次の2段階で自動選択される
     1. VTTの話者名(Zoomの表示名)を、登録済みの受講者名と照合する。1人に絞れたら選択し、
        バッジに「話者名から自動選択」と出る(この場合AIは呼ばれない)
     2. 照合で決まらなかったカードだけ、まとめてAIに投げる。話者名と会話の冒頭だけを送り、
        登録済みの企業×受講者の組み合わせの中から選ばせる。決まったカードのバッジは「AIが推定」
   - どちらでも決まらなかったカードは「対象者を選んでください」と赤いバッジが出るので、
     プルダウンから手動で選ぶ(手動で直すとバッジは「手動で選択」に変わる)
4. **自動選択はあくまで下書きなので、書き込む前に全カードの企業・受講者を目視で確認する。**
   AIが候補リストに無い組み合わせを返してきた場合はサーバー側で捨てるため、
   「登録されていない誰か」に書き込まれることはないが、**登録済みの別人と取り違える可能性はある**
5. 「AIで要約をまとめて作成」を押すと、カードを上から順に1件ずつ要約する(件数×数十秒かかる)。
   進捗は画面下に「(2/5)」のように出て、各カードにも結果が表示される。
   実施日が空のカードはスキップされるので、先に埋めておくこと
6. 各カードの記録内容を確認・修正する
7. 「この内容でまとめて書き込む」を押すと、全カードが1回でそれぞれの受講者の列に追記される。
   記録内容が空のカード・対象者が未選択のカードは書き込まれず、スキップとして報告される

その他:
- 「AIで対象者を推定し直す」… 全カードをもう一度AIに投げ直す(手動で選んだものも上書きされる)
- 「読み込んだファイルを全て消す」… カードを全消去してやり直す
- 各カードの「✕ 削除」… そのファイルだけ対象から外す

### 3-5-2. 使い方(進捗を確認)

「進捗を確認」タブでは、書き込み済みの記録をそのまま閲覧できる(絞り込みながら状況を追う用途)。
**確認するだけの画面なので、実行ボタンは置かず、選んだ時点で自動的に表示される作りにしている。**

1. タブを開くと、先頭の企業が自動で選ばれ、左に受講者一覧(記録件数・直近日付つき)、
   右に先頭の受講者の全記録が新しい順(直近が上)で表示される。**ここまでクリック0回。**
2. 左の受講者をクリックすると、右がその人の全記録に切り替わる
3. 「表示対象」プルダウンで企業を切り替えると、その場で左右とも読み込み直される。
   このプルダウンには企業と、集団相談用に保存した「グループ」の両方が(見出しで分けて)入っており、
   グループを選ぶと企業をまたいだメンバー全員が左に並ぶ(所属企業名も各行に表示される)
4. 右上の「一覧 / 表」で表示を切り替えられる。「表」は回数(1回目・2回目...)を縦、受講者を
   横並びにした一覧表(受講者ごとに実施日はズレていてもよく、各受講者にとっての「何回目の
   記録か」で行を揃えている)。グループ表示中は表に切り替えられない(企業をまたぐため)
5. シートや受講者が見つからないグループメンバーは、左の一覧にその旨が表示され、
   自動選択の対象からは外れる(正常なメンバーが先に選ばれる)

### 3-5-3. 使い方(登録・管理)

「登録・管理」タブでは、新しい企業・受講者の登録と、集団相談用の「グループ」管理ができる。

**企業・受講者の登録**

1. 「新しい企業名」に企業名を入力し、「企業を登録」を押すとシートが新規作成される
   (この時点では受講者が0人なので、「記録を追加」「進捗を確認」タブのプルダウンにはまだ
   出てこない。ただし、この「企業(シート)」プルダウン自体には受講者0人でも出るので、
   続けて受講者を登録できる)
2. 続けて「企業(シート)」で今登録した企業を選び、「新しい受講者名」に受講者名を入力して
   「受講者を登録」を押す。**複数人まとめて登録する場合は1行に1人ずつ**入力する(例:
   1行目「山田太郎」、2行目「鈴木花子」)。登録結果は1人ずつ✅/❌で表示され、一部が
   同名重複などで失敗しても他の人の登録はそのまま反映される。受講者を1人登録すると、
   「記録を追加」「進捗を確認」タブのプルダウンにも反映される

**グループ管理(集団相談用)**

同じ組み合わせで繰り返し行う集団相談を「グループ」として保存しておける(企業をまたいでもよい)。

1. 「+ メンバーを追加」で行を増やし、それぞれ企業・受講者をプルダウンで選ぶ
2. 「グループ名」を入力し、「このメンバーでグループを保存」を押す
3. 保存したグループは一覧にカード表示され、「記録追加に使う」を押すと「記録を追加」タブに
   切り替わり、そのグループの全メンバー分の対象者欄が自動で用意される(あとはVTTを選んで
   要約するか、記録内容を直接入力して書き込むだけでよい)
4. 同じ名前で保存し直すとメンバーが上書きされる。「削除」でグループ自体を削除できる

### 3-5-4. 次回日程一覧

次回の相談予定を機械的に読める形で持っておくための仕組み。次回日程は「【次回に向けて】」の
自由記述に埋もれてしまい他ツールから参照できないため、自由記述とは別に構造化して保存する。

- **入力** … 「記録を追加」タブ(**1件ずつモードのみ**)の「次回相談予定日」に、日付・開始・終了を
  入れて書き込むと、`次回日程一覧` シートへ自動で反映される。複数VTT一括モードは対象外なので、
  一括で書き込んだあとに1件ずつモードから個別に追記する
- **保存先** … `次回日程一覧` という管理シート(無ければ自動作成)。
  列は 企業名 / 受講者 / 次回日付 / 開始 / 終了 / 記録日(今回の相談日) / 更新日時。
  **企業名+受講者名で1行**なので、同じ人の予定を入れ直すとその行が上書きされる(行は増えない)
- **閲覧** … 「登録・管理」タブ下部の「次回日程一覧を見る(全社)」で、次回日付が早い順に一覧できる。
  今日より前の日付は「日付経過(未更新の可能性)」として色付きで表示される
- **他ツールからの参照** … POODLE自動登録などから使う場合は、Webアプリを経由せず
  `次回日程一覧` シートを直接読めばよい
- 予定を取り消したい場合は、シートの該当行を削除するか「次回日付」欄を空にする
  (空の行は一覧に出てこない)
- `次回日程一覧` と `グループ設定` は管理用シートなので、企業(受講者)一覧のプルダウンには出てこない。
  この名前で企業を新規登録することもできない

### 3-6. 補足

- 企業(シート)・受講者の指定について:
  - 「1件ずつ」モードでは自動推定しない。必ず自分でプルダウンから選ぶ(表記ゆれによる誤書き込みを防ぐため)
  - 「複数VTTを一括」モードだけは、件数が多くて手で選ぶのが現実的でないため自動選択を入れている。
    ただし**選ぶのはプルダウンの初期値だけ**で、書き込みはユーザーが確認して押すまで行われない。
    またAIには「登録済みの企業×受講者の組み合わせ」を候補として渡し、候補に無い組み合わせが
    返ってきた場合はサーバー側(`estimateTargets`)で捨てる
- 要約が明らかにおかしい場合は、記録内容欄を直接編集してから書き込んでよい
- AとBのどちらを使うかはケースバイケースでよい。Bが使えない・止まっている場合はAにフォールバックできる
- グループの定義は、スプレッドシート内に自動作成される「グループ設定」というシートに保存される。
  直接編集も可能だが、通常はWebアプリの「登録・管理」タブから操作すれば十分(このシートは
  企業・受講者のプルダウンには表示されない)
